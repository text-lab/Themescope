% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/visualization.R
\name{plot_network}
\alias{plot_network}
\title{Plot a community-coloured semantic network}
\usage{
plot_network(
  graph,
  membership,
  community_labels = NULL,
  layout = "fr",
  top_n_labels = 10,
  ...
)
}
\arguments{
\item{graph}{An \code{igraph} object.}

\item{membership}{Named integer vector of community assignments (as returned
by \code{\link{detect_communities}}).}

\item{community_labels}{Named character vector mapping community IDs to
display labels. Optional.}

\item{layout}{Character. Graph layout algorithm passed to igraph layout
functions or \pkg{ggraph}. One of
\code{"fr"} (Fruchterman-Reingold), \code{"kk"} (Kamada-Kawai),
\code{"dh"} (Davidson-Harel), \code{"lgl"} (LGL). Default \code{"fr"}.}

\item{top_n_labels}{Integer. Number of highest-degree nodes per community to
label (default \code{10}).}

\item{...}{Additional arguments passed to the plotting function.}
}
\value{
A \code{\link[ggplot2]{ggplot}} object (if \pkg{ggraph} is
available) or invisibly \code{NULL} after a base-R plot (if not).
}
\description{
Visualises the co-occurrence network with nodes coloured by community
membership. Attempts to use \pkg{ggraph} for a ggplot2-based output; falls
back to base-R \code{igraph::plot.igraph} if \pkg{ggraph} is not installed.
}
\examples{
\dontrun{
plot_network(graph, comm$membership, top_n_labels = 5)
}

}
