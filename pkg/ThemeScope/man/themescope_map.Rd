% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/visualization.R
\name{themescope_map}
\alias{themescope_map}
\title{Create a ThemeScope representational map}
\usage{
themescope_map(
  psi,
  cs,
  community_labels = NULL,
  community_sizes = NULL,
  title = "ThemeScope Map",
  highlight = NULL,
  palette = NULL,
  ...
)
}
\arguments{
\item{psi}{Named numeric vector of PSI values (one per community).}

\item{cs}{Named numeric vector of CS values (one per community). May contain
\code{NA} for communities without concreteness data.}

\item{community_labels}{Named character vector of display labels for
communities. If \code{NULL}, community names from \code{psi} are used.}

\item{community_sizes}{Named numeric vector of community sizes used to scale
point area. If \code{NULL}, all points are the same size.}

\item{title}{Character. Plot title (default \code{"ThemeScope Map"}).}

\item{highlight}{Character vector of community names to highlight with a
distinct colour.}

\item{palette}{Character vector of colours. If \code{NULL}, a default
palette is used.}

\item{...}{Additional arguments (currently unused).}
}
\value{
A \code{\link[ggplot2]{ggplot}} object.
}
\description{
Produces a two-dimensional scatter plot locating each community in the
theoretical space defined by z-scored PSI (x-axis, anchoring) and z-scored
CS (y-axis, objectification). The four quadrants correspond to Social
Representation Theory constructs.
}
\examples{
\dontrun{
themescope_map(psi = ts$psi, cs = ts$cs,
              community_sizes = sapply(ts$communities, length))
}

}
